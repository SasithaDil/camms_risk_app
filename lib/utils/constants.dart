import 'package:flutter/material.dart';
import 'package:risk_sample/core/theme.dart';

class UI {
  static const padding = 8.0;
  static const padding2x = 16.0;
  static const padding3x = 24.0;
  static const padding4x = 32.0;
  static const padding8x = 64.0;
}

class TextStrings {
  static const textBoxString = 'GroupHeader';
  static const dateTime = 'DateTime';
  static const comboButtons = 'ComboButtons';
}

class TextStyles {
  static const boldText = TextStyle(fontWeight: FontWeight.bold);
  static const titleText =
      TextStyle(fontSize: 23.0, color: mainBlue, fontWeight: FontWeight.w500);
}
